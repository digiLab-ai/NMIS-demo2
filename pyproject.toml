[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "cryo-isotope-gp-demo"
version = "0.1.0"
description = "Streamlit demo for fake-CFD cryogenic isotope separation GP emulator"
authors = [{name = "Demo Generator"}]
dependencies = []
